# develop-in-docker: postgres
base on ubuntu image

# features:
* openssh-server
* sudo
* postgres

# usage:
* ssh connect to container:
    ```
    ssh ubuntu@localhost -p 10022
    ```
* can use postgres in ssh container
